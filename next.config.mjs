/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: {
            allowedOrigins: ["solid-capybara-r55x65w9vw4hpg9r-3000.app.github.dev"]
        }
    }
};

export default nextConfig;
